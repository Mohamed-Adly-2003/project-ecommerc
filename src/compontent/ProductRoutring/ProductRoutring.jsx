import React from 'react'
import { Navigate } from 'react-router-dom';

export default function ProductRoutring({ childern }) {
    // if (localStorage.getItem("token")) {
    //     return childern;

    // } else {
    //     return <Navigate to="<Login />"  />
    // }
}
